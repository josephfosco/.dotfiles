# alias emacs to use terminal version
alias emacs='emacs -nw'

# alias tmux to start forcing a 256 color terminal
alias tmux='tmux -2'

# make vim the default editor (vi invokes vim)
# commented out because it makes tmux use vi mode
#  export VISUAL=vi
# export EDITOR="$VISUAL"
